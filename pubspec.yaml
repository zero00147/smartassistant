name: smart_assistant
description: Smart Assistance using AI ChatBot
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.2.0 <4.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0   # for loading the token from .env

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  #For picking audio/video files from storage
  file_picker: ^8.1.2

  #For recording audio directly inside the app
  record: ^6.1.2

  #For managing permissions (mic, storage, etc.)
  permission_handler: ^11.3.1

  #For accessing file paths and saving files
  path_provider: ^2.1.3

  #For showing chat messages in a beautiful UI
  chat_bubbles: ^1.6.0

  #For speech-to-text (if you want real-time recognition)
  speech_to_text: ^7.0.0
  shared_preferences: ^2.5.3

  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
